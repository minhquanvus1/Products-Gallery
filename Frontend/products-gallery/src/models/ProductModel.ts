export class ProductModel {
    constructor(public id: number, public title: string, public images: string[], public category: string, public description: string, public rating: number, public price: number) {}
}